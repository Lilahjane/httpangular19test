import { RecipeListComponent } from './../recipe_components/recipe-list/recipe-list.component';
import { Routes } from '@angular/router';

export const routes: Routes = [
    {
        path: 'landingpage',
        component: RecipeListComponent
    },
];
